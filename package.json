{
  "dependencies": {
    "aws-sdk": "^2.1330.0",
    "cognito-express": "^3.0.2",
    "cypress": "^12.9.0",
    "express": "^4.18.2",
    "serverless-cognito-add-custom-attributes": "^0.3.1",
    "serverless-http": "^3.1.1"
  },
  "devDependencies": {
    "eslint": "^8.35.0",
    "eslint-config-google": "^0.14.0",
    "serverless-dynamodb-local": "^0.2.40",
    "serverless-offline": "^12.0.4",
    "serverless-offline-local-authorizers-plugin": "^1.2.0"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  },
  "scripts": {
    "lint": "eslint './**/*.js' --fix",
    "test": "cross-env NODE_ENV=test IS_OFFLINE=true jest --testTimeout=10000"
  }
}
